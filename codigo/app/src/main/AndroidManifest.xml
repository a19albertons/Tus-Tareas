<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".TusTareasApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.TusTareas.Splash">

        <!-- Habilita el profileable para la aplicación, lo que permite ver el consumo real de recuros para la herramienta profiler -->
        <!-- Exigimos el api 29 para esta opción por culpa del min SDK -->
        <profileable
            android:shell="true"
            tools:targetApi="29"
            />

        <receiver
            android:name=".workers.LanzarNotificaciones"
            android:enabled="true"
            android:exported="false"
            />
        <receiver android:name=".workers.RestaurarAlarmasReinicio"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- activity para errores no previstos -->
        <activity
            android:name=".ErrorNoPrevisto"
            android:process=":error_process"
            android:theme="@style/Theme.TusTareas" />
        <!-- Necesario para persistir el idioma -->
        <service
            android:name="androidx.appcompat.app.AppLocalesMetadataHolderService"
            android:enabled="false"
            android:exported="false">
            <meta-data
                android:name="autoStoreLocales"
                android:value="true" />
        </service>
    </application>

</manifest>